# Server
PORT=5000
ALLOWED_ORIGINS=https://anointed-flames-channel.onrender.com

# Database (REPLACE with your actual Render PostgreSQL URL)
DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DB_NAME

# Authentication
JWT_SECRET=your_real_secure_jwt_key
JWT_EXPIRES_IN=7d

# Upload Settings
UPLOAD_LIMIT_MB=1024
STORAGE_PATH=./storage
